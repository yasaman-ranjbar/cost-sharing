# 💰 محاسبه‌گر هزینه‌های دوره‌همی

یک برنامه وب برای تقسیم عادلانه هزینه‌های گردهمایی‌های خانوادگی بر اساس تعداد افراد هر خانواده.

## 📋 ویژگی‌ها

- ✅ ثبت خانواده‌ها و تعداد افراد هر خانواده
- ✅ ثبت هزینه‌ها با مشخص کردن خانواده پرداخت‌کننده
- ✅ محاسبه خودکار سهم هر خانواده بر اساس تعداد افراد
- ✅ نمایش جدول تسویه حساب و مانده بدهی/طلب هر خانواده
- ✅ ذخیره‌سازی خودکار اطلاعات در مرورگر (Local Storage)
- ✅ رابط کاربری زیبا و کاربرپسند با Tailwind CSS
- ✅ پشتیبانی کامل از زبان فارسی و واحد پولی تومان

## 🛠️ تکنولوژی‌های استفاده شده

- **React 19.2** - کتابخانه اصلی UI
- **Vite 7.2** - ابزار بیلد و توسعه
- **Tailwind CSS 4.1** - فریمورک CSS
- **Local Storage API** - ذخیره‌سازی داده‌ها

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Node.js (نسخه 16 یا بالاتر)
- npm یا yarn

### مراحل نصب

1. نصب وابستگی‌ها:

```bash
npm install
```

2. اجرای برنامه در حالت توسعه:

```bash
npm run dev
```

3. باز کردن مرورگر و مراجعه به آدرس نمایش داده شده (معمولاً `http://localhost:5173`)

### دستورات دیگر

```bash
# ساخت نسخه پروداکشن
npm run build

# پیش‌نمایش نسخه پروداکشن
npm preview

# بررسی کد با ESLint
npm run lint
```

## 📖 نحوه استفاده

### مرحله 1: افزودن خانواده‌ها

- نام خانواده و تعداد افراد را وارد کنید
- روی دکمه "افزودن خانواده" کلیک کنید
- می‌توانید خانواده‌های بیشتری اضافه کنید

### مرحله 2: ثبت هزینه‌ها

- نام کالا یا خدمت را وارد کنید
- مبلغ را به تومان وارد کنید
- خانواده‌ای که هزینه را پرداخت کرده است را انتخاب کنید
- روی دکمه "افزودن هزینه" کلیک کنید

### مرحله 3: مشاهده لیست هزینه‌ها

- لیست کامل هزینه‌های ثبت شده
- مجموع کل هزینه‌ها

### مرحله 4: محاسبات و تسویه حساب

- جدول کامل تقسیم هزینه‌ها بین خانواده‌ها
- مبلغ پرداختی هر خانواده
- سهم واقعی هر خانواده (بر اساس تعداد افراد)
- مانده بدهی یا طلب هر خانواده

## 📁 ساختار پروژه

```
cost-sharing-app/
├── src/
│   ├── components/
│   │   ├── FamilyManager.jsx      # مدیریت خانواده‌ها
│   │   ├── ExpenseManager.jsx     # مدیریت هزینه‌ها
│   │   ├── ExpenseList.jsx        # نمایش لیست هزینه‌ها
│   │   └── SettlementTable.jsx    # جدول تسویه حساب
│   ├── App.jsx                     # کامپوننت اصلی
│   ├── index.css                   # استایل‌های سراسری
│   └── main.jsx                    # نقطه ورود برنامه
├── public/                         # فایل‌های استاتیک
├── index.html                      # HTML اصلی
├── package.json                    # وابستگی‌ها و اسکریپت‌ها
├── vite.config.js                  # تنظیمات Vite
├── tailwind.config.js              # تنظیمات Tailwind CSS
└── README.md                       # این فایل
```

## 💾 ذخیره‌سازی داده‌ها

برنامه به صورت خودکار تمام اطلاعات خانواده‌ها و هزینه‌ها را در Local Storage مرورگر ذخیره می‌کند. این به معنای آن است که:

- اطلاعات شما با رفرش کردن صفحه از بین نمی‌رود
- داده‌ها فقط در مرورگر شما ذخیره می‌شود (بدون ارسال به سرور)
- برای پاک کردن داده‌ها، باید Cache مرورگر را پاک کنید

## 🎨 ویژگی‌های طراحی

- طراحی ریسپانسیو برای موبایل و دسکتاپ
- انیمیشن‌های نرم و جذاب
- رنگ‌بندی حرفه‌ای و مدرن
- پشتیبانی کامل از راست‌چین (RTL) برای فارسی
- فرمت‌بندی خودکار اعداد با جداکننده هزارگان

## 🤝 مشارکت

برای مشارکت در این پروژه:

1. پروژه را Fork کنید
2. یک Branch جدید بسازید (`git checkout -b feature/AmazingFeature`)
3. تغییرات خود را Commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. به Branch خود Push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request باز کنید

## 📝 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 👨‍💻 سازنده

ساخته شده با ❤️ برای تسهیل تقسیم هزینه‌های گردهمایی‌های خانوادگی
